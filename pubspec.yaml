name: easy_skeleton
description: Skeleton is easy.
version: 0.10.1
repository: https://github.com/karbunkul/flutter_skeleton

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0