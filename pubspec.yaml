name: easy_skeleton
description: Skeleton is easy.
version: 0.9.2
repository: https://github.com/karbunkul/flutter_skeleton

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: any