name: easy_skeleton
description: Skeleton is easy.
version: 0.10.2
repository: https://github.com/karbunkul/flutter_skeleton

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3